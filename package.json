{
  "name": "ethevents",
  "version": "1.0.0",
  "description": "Server-side application for ETH events API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check index.js && node --check event-utils.js",
    "test": "node --test",
    "audit": "npm audit --audit-level=high"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1"
  }
}
